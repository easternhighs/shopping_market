# AGENTS.md

本文件是给本仓库里的编码代理（Agent）使用的长期工作规则，适用范围是整个项目目录。与用户沟通默认使用中文。

## 1. 以构建文档为准
- 项目开发必须依据项目内的构建文档来推进。
- 构建文档位置：`docs/build.md`。
- 开始任何开发、搭建、修改之前，先读取并遵循构建文档里的计划、阶段顺序、模块边界和验收标准。
- 如果构建文档与代码现状不一致，以构建文档为构建依据；除非用户明确要求更新文档或改变方向，否则不要自行偏离。

## 2. 用新手听得懂的话解释
- 解释模块的功能和作用时，要尽量直白、通俗，把用户当成完全不了解的新手。
- 不要默认用户理解 Go、MySQL、Redis、Kafka、微服务、并发、事务等概念。
- 解释顺序优先是：这个模块解决什么问题 → 它做了什么 → 它和其他模块怎么配合。
- 能打比方就打比方，少堆术语；必须使用术语时，先解释含义。

## 3. 按新手节奏逐步搭建，并汇报进展
- 项目构建按照“完全不了解的新手”的节奏进行，一个模块、一个阶段地逐步完成，不要一次铺开。
- 每次重新开启项目（新的会话/任务）时，先读取 `docs/progress.md`，然后向用户汇报：
  - 上次完成了什么；
  - 下次计划完成什么。
- 每完成一个阶段或模块，及时更新 `docs/progress.md`，保持进度记录准确。

## 4. 用“挖空”方式让用户动手，不擅自完成模块
- 在具体模块的实现上，尽量采用“挖空”的形式：搭好骨架、留出待实现的关键函数或逻辑，让用户实际动手完成。
- 可以用注释给出提示、思路或示例，例如 `// TODO(你来实现)：...`。
- 除非用户明确直接命令“帮我把这个模块完成”，否则不要自行把模块直接写完。
- 用户动手后，可以先帮检查、解释、指出问题，但仍以用户完成为主。

## 5. 阶段完成后提交 git
- 每完成构建文档中的一个阶段后，先把该阶段完成的内容做总结，再用这段总结作为提交说明（commit message）提交到 git，更新项目版本。
- 提交前先检查 `git status`，确认要提交的文件属于当前阶段的改动。
- 只提交当前阶段相关的文件，不提交本地缓存、临时文件等无关内容；必要时先更新 `.gitignore`。
- 提交说明要清晰概括本阶段做了什么，例如 `feat: 完成阶段1用户模块（注册/登录/JWT）`。
- 如果项目还不是 git 仓库，或用户明确要求不要提交，则跳过并说明原因。
