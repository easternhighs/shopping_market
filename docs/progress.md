# 项目进度记录

每次重新开启项目时，先读这里并向用户汇报“上次完成 / 下次计划”。完成一个模块后更新对应状态。

## 当前状态

- 当前阶段：阶段 6（进行中）
- 上次完成：消费端吞吐优化（4 处挖空已由你填写完毕）跑通后，压测发现新的瓶颈在 MySQL 行锁上——消费速率只有约 80 条/秒，`Innodb_row_lock_waits` 几乎等于 `Com_commit`，平均每次等锁 65 毫秒，因为每条消息一个事务、都要 `UPDATE` 同一行库存
- 本次完成：批量落库已全部实现（`DeductStockBatch` 一条 SQL 扣整批 + `processOrdersBatch` 整批一个事务：批量查重、批量查限购、按 itemID 汇总扣库存、批量插订单），并通过三轮压测验证：c=100/200/600 下 QPS 约 1900～2000、全部 200 且 0 错误，Kafka LAG 全程基本为 0；15379 条消息只用 196 次提交（约 78 条/事务），行锁等待从 1057 降到 90
- 收尾核对：多轮压测累积后做了一次总对账，商品 `id=14`（总库存 200000）的 `sold`、订单数、去重用户数都是 105092，Redis 剩余库存 94908 = 200000 - 105092，四个数字完全自洽，没有超卖也没有重复购买；另外用 `information_schema.STATISTICS` 复核，确认模型声明的唯一索引在库里全部是 UNIQUE（之前 `SHOW INDEX` 的怀疑是误读，`Non_unique=0` 表示唯一），防重兜底是成立的
- 文档产出：新增 `docs/architecture.md`（整体部署图、代码分层、秒杀时序图、模块分工、Redis Key 设计、接口清单、三层防线、后台任务、数据库选型对比、微服务拆分边界）和 `docs/interview.md`（90 秒开场话术、四轮优化故事、数据手册、9 个高频追问准备、不足与后续规划）
- 下次计划：阶段 6 只剩最后一件事——冲目标 QPS（约 1 万）。当前卡在压测客户端/入口路径（Redis Lua + Kafka 发送），需要换更强的压测方式或继续优化入口；动手前先评估改动风险，并由你确认后保留可回退的 git 提交点

## 阶段进度

- [x] 阶段 0：环境准备与项目骨架
- [x] 阶段 1：用户模块（注册/登录）
- [x] 阶段 2：商品与库存模块
- [x] 阶段 3：购物车与普通下单
- [x] 阶段 4：秒杀核心模块
- [x] 阶段 5：高并发增强
- [ ] 阶段 6：压测与优化（进行中）
